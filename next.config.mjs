/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      // Enable loading of images from local assets
      unoptimized: true, // Optional: If you're having issues with image optimization
    },
  };
  
  export default nextConfig;
  